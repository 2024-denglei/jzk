# 精子库匹配（JZK）

面向求诊者的捐精人匹配系统：用户用自然语言描述期望，系统据此在捐精人库中筛选并排序候选，通过分支式对话逐步细化。

本文件只是术语表，用来固定团队用词。它不描述实现，也不记录架构决策——后者写进 `docs/adr/`。

## 领域对象

**捐精人**：
库中一条可被匹配的候选记录，对外以 `code` 标识。
_Avoid_: 供体、donor 记录、样本

**偏好画像**：
用户对理想捐精人的一组期望，是发起一次匹配所需的完整输入。
_Avoid_: 偏好、条件、筛选项、profile

**硬过滤条件**：
偏好画像中不允许妥协的部分，不满足即排除候选，不参与打分。
_Avoid_: must 条件、必选项、硬性要求

**匹配快照**：
某次匹配产出的候选名次的冻结副本，之后翻页与回看都读它，不重新计算。
_Avoid_: 匹配结果、结果集、result set

## 匹配与筛选

**匹配**：
一个完整用例：输入一份偏好画像，产出一份匹配快照。由顾问在对话中发起。
_Avoid_: 推荐、检索、搜索

**打分**：
模型为单个候选算出一个分值。这是匹配的一个环节，也是一项独立的对外能力。
_Avoid_: 评分、算分

**排序**：
按分值为候选定出名次，是产出匹配快照的一步。
_Avoid_: 排名、rank、名次计算

**筛选**：
用户自己指定条件在捐精人库中查询，不经顾问、不经模型。它与匹配是两项不同的能力，不是匹配的简化版。
_Avoid_: 搜索、search、用「匹配」称呼筛选

## 对话

**对话**：
用户与顾问之间的一段完整会话。系统只有一种对话存储，谈论它时不带版本号。
_Avoid_: chat-v2、v2 对话、会话

**分支**：
对话中因编辑历史消息而岔出的一条独立消息序列。同一对话可有多条分支。
_Avoid_: 分叉、fork、变体

**回合**：
一次用户发言及其引发的顾问回应，是对话的最小推进单位。
_Avoid_: turn、轮次、消息对

**生成**：
一次顾问回应的异步产出过程，可被查询状态、订阅事件、中途停止。
_Avoid_: 任务、run、请求

**顾问**：
面向用户的 AI 角色，负责理解偏好、调用匹配、解释结果。
_Avoid_: agent、助手、机器人

## 模型与契约

**模型**：
神经网络本身。这个词**只**指神经网络，不指数据形状、不指数据库表。
_Avoid_: 用「模型」称呼 Pydantic 类或数据库表

**检查点**：
模型训练产出的权重文件，附带可校验的身份信息。
_Avoid_: 模型文件、权重、pt 文件、model

**契约**：
跨边界传递数据的形状定义——HTTP 请求响应、服务间调用、持久化载荷。
_Avoid_: model、DTO、数据模型

**Schema**：
数据库中表与列的结构定义。
_Avoid_: 数据模型、表模型
