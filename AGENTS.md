# Agent 工作规范

面向本仓库协作的 AI / Agent，必须遵守以下交付纪律。

## 1. 每次改动完成后必须创建 Git Commit

- 完成一轮可交付的代码改动后，**必须**创建 git commit，便于后续追踪与回滚。
- 提交信息应简洁说明「为什么改」，而不是只罗列文件名。
- 未经用户明确要求，不要 `push --force`、改写已推送历史，或跳过 hooks。
- 不要提交密钥、`.env` 中的真实凭证等敏感信息。

## 2. 每次改动后必须补充测试并验证通过

- 每次改动后，**必须**编写或更新相关测试（单测、接口测或与改动对等的验证）。
- 在交付给用户前，**必须**跑通相关测试与必要验证，确认全部通过。
- 若因环境限制无法执行某项测试，需在回复中明确说明未跑项与原因，并尽量用可替代检查补齐。
