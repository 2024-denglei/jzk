# 统一匹配 HTTP 接口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 增加 `POST /api/match`，校验 PreferenceProfile 后内部完成 must 过滤与打分排序。

**Architecture:** 薄路由包装 `parse_profile` + `match_profile`；JWT 用 `get_current_user_id`；`top_k` 在路由层切片。对话不改。

**Tech Stack:** FastAPI、pytest、现有 `core.preference`。

规格：`docs/superpowers/specs/2026-08-25-match-api-design.md`

## 文件

- Create: `api/match.py`
- Create: `tests/test_match_api.py`
- Modify: `main.py` 注册路由

### Task 1: 测试 + 实现 `/api/match`

- [ ] 写失败测试（401/400/空画像/排序/0 命中/top_k）
- [ ] 实现路由并挂到 main
- [ ] pytest 通过
